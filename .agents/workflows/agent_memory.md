---
name: Agent Memory
description: Decide what an agent may persist, retrieve, expire, and cite as durable memory
trigger: /agent_memory
---

# Agent Memory

Store only information that has a stable future use. Keep provenance, scope, timestamp, confidence, and deletion rules with each durable memory. Separate task-local scratch state from durable preferences and project facts. Never treat recalled memory as stronger evidence than current authoritative sources.
