---
name: Add Agent
description: Add a new agent role with clear ownership, handoff rules, tool limits, and tests
trigger: /add_agent
---

# Add Agent

Document why a new agent is needed instead of extending an existing role. Define its responsibility, non-responsibilities, required context, allowed tools, permissions, handoff protocol, success criteria, and test cases. Avoid overlapping write authority between agents.
